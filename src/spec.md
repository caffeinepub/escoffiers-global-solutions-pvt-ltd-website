# Specification

## Summary
**Goal:** Display the Escoffiers logo and the visible company name text together across header, footer, and Home hero branding while keeping layout and navigation behavior unchanged.

**Planned changes:**
- Update the site header left brand area to show the existing Escoffiers logo image (`/assets/generated/escoffiers-wordmark.dim_1200x300.png`) plus visible text “Escoffiers”, keep the entire brand area clickable linking to `/`, and ensure vertical centering within the `h-16` header across desktop and mobile.
- Update the site footer branding block to show the same logo image plus visible text “Escoffiers”, aligned and readable across responsive breakpoints without changing footer links/contact behavior.
- Update the Home page hero branding block to add the same logo image plus visible text “Escoffiers” above the hero headline, keeping the hero headline text and layout otherwise unchanged.

**User-visible outcome:** Users see the Escoffiers logo and the name “Escoffiers” together in the header, footer, and Home hero area, and clicking the header brand area takes them to the Home page.
